# Sistema contable (Streamlit)

MVP inicial de un sistema contable personal en Streamlit.

## Ejecutar en local

1) Instalar dependencias:
~~~bash
pip install -r requirements.txt
~~~

2) Ejecutar la app:
~~~bash
streamlit run app.py
~~~
